<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFrameworks Condition="$([MSBuild]::IsOSPlatform('windows'))">net10.0-windows10.0.19041.0</TargetFrameworks>
		<TargetFrameworks Condition="$([MSBuild]::IsOSPlatform('osx'))">net10.0-maccatalyst</TargetFrameworks>
		<!-- Uncomment to also build the tizen app. You will need to install tizen by following this: https://github.com/Samsung/Tizen.NET -->
		<!-- <TargetFrameworks>$(TargetFrameworks);net10.0-tizen</TargetFrameworks> -->

		<!-- Skip Xcode version check - we have Xcode 26.1.1 which is compatible -->
		<VerifyXcodeVersion>false</VerifyXcodeVersion>

		<!-- Note for MacCatalyst:
		The default runtime is maccatalyst-x64, except in Release config, in which case the default is maccatalyst-x64;maccatalyst-arm64.
		When specifying both architectures, use the plural <RuntimeIdentifiers> instead of the singular <RuntimeIdentifier>.
		The Mac App Store will NOT accept apps with ONLY maccatalyst-arm64 indicated;
		either BOTH runtimes must be indicated or ONLY macatalyst-x64. -->
		<!-- For example: <RuntimeIdentifiers>maccatalyst-x64;maccatalyst-arm64</RuntimeIdentifiers> -->

		<OutputType>Exe</OutputType>
		<AssemblyName>POS-in-NET</AssemblyName>
		<RootNamespace>POS_in_NET</RootNamespace>
		<UseMaui>true</UseMaui>
		<SingleProject>true</SingleProject>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>
		<!-- Local verification builds are written here and must never be compiled as source. -->
		<DefaultItemExcludes>$(DefaultItemExcludes);artifacts\**</DefaultItemExcludes>
		
		<!-- Suppress platform-specific and nullable reference warnings -->
		<NoWarn>CA1416;CS8892;CS1998;CS8604;CS8601;CS0067</NoWarn>

		<!-- Display name -->
		<ApplicationTitle>OrderWeb POS</ApplicationTitle>

		<!-- App Identifier -->
		<ApplicationId>com.orderweb.pos</ApplicationId>

		<!-- Release versions: increment ApplicationVersion for every release. -->
		<ApplicationDisplayVersion>1.0.1</ApplicationDisplayVersion>
		<ApplicationVersion>2</ApplicationVersion>
		<Version>1.0.1</Version>
		<AssemblyVersion>1.0.1.0</AssemblyVersion>
		<FileVersion>1.0.1.0</FileVersion>

		<!-- To develop, package, and publish an app to the Microsoft Store, see: https://aka.ms/MauiTemplateUnpackaged -->
		<WindowsPackageType>None</WindowsPackageType>

		<SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'ios'">15.0</SupportedOSPlatformVersion>
		<SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'maccatalyst'">15.0</SupportedOSPlatformVersion>
		<!-- Windows 10 22H2 and Windows 11 are supported production till platforms. -->
		<SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'windows'">10.0.19041.0</SupportedOSPlatformVersion>
		<TargetPlatformMinVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'windows'">10.0.19041.0</TargetPlatformMinVersion>
		<SupportedOSPlatformVersion Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'tizen'">6.5</SupportedOSPlatformVersion>
	</PropertyGroup>

	<PropertyGroup Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'windows' and '$(RuntimeIdentifierOverride)' != ''">
		<RuntimeIdentifier>$(RuntimeIdentifierOverride)</RuntimeIdentifier>
	</PropertyGroup>

	<ItemGroup>
		<!-- App Icon -->
		<MauiIcon Include="Resources\AppIcon\companyappicon.png" />

		<!-- Splash Screen -->
		<MauiSplashScreen Include="Resources\Images\companymark.png" Color="#FFFFFF" BaseSize="256,256" />

		<!--
			Fixed application images are owned by OrderWeb.SharedUI and linked into
			each host app. The legacy local files remain on disk only as a rollback
			copy during the safe migration.
		-->
		<MauiImage Include="..\OrderWeb.SharedUI\Assets\Fixed\**\*.png"
				   Link="Resources\Images\%(Filename)%(Extension)" />
		<MauiImage Include="..\OrderWeb.SharedUI\Assets\Fixed\**\*.svg"
				   Link="Resources\Images\%(Filename)%(Extension)" />
		
		<!-- Custom Fonts: app font aliases are registered in MauiProgram.cs. -->
		<MauiFont Include="Resources\Fonts\OpenSans-Regular.ttf" />
		<MauiFont Include="Resources\Fonts\OpenSans-Semibold.ttf" />

		<!-- Raw Assets (also remove the "Resources\Raw" prefix) -->
		<MauiAsset Include="Resources\Raw\**" LogicalName="%(RecursiveDir)%(Filename)%(Extension)" />
	</ItemGroup>

	<ItemGroup>
		<FrameworkReference Include="Microsoft.AspNetCore.App" />

		<!-- Exclude future services from compilation -->
		<Compile Remove="FutureServices\**" />
		<EmbeddedResource Remove="FutureServices\**" />
		<None Remove="FutureServices\**" />
		<!-- Client, tools, and tests are separate projects outside this source tree. -->
		<!-- Shared UI is compiled by its own project and consumed through ProjectReference. -->
		<Compile Remove="OrderWeb.SharedUI\**" />
		<MauiXaml Remove="OrderWeb.SharedUI\**" />
		<EmbeddedResource Remove="OrderWeb.SharedUI\**" />
		<None Remove="OrderWeb.SharedUI\**" />
	</ItemGroup>

	<ItemGroup>
		<ProjectReference Include="..\OrderWeb.SharedUI\OrderWeb.SharedUI.csproj" />
	</ItemGroup>

	<ItemGroup>
		<PackageReference Include="Microsoft.Maui.Controls" Version="$(MauiVersion)" />
		<PackageReference Include="Microsoft.WindowsAppSDK" Version="2.2.0" Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'windows'" />
		<!-- Use MySqlConnector instead of MySql.Data for better cross-platform support -->
		<PackageReference Include="MySqlConnector" Version="2.6.1" />
		<PackageReference Include="Newtonsoft.Json" Version="13.0.3" />
		<PackageReference Include="BCrypt.Net-Next" Version="4.0.3" />
		<PackageReference Include="System.Security.Cryptography.ProtectedData" Version="10.0.0" />
		<!-- ASP.NET Core for webhook server -->
		
		<!-- .NET MAUI Community Toolkit - Enhanced controls and behaviors -->
		<PackageReference Include="CommunityToolkit.Maui" Version="14.2.0" />
		<PackageReference Include="CommunityToolkit.Mvvm" Version="8.4.2" />
		
		<!-- Syncfusion .NET MAUI UI Controls (Essential Studio UI Edition Binary License v34.x) -->
		<PackageReference Include="Syncfusion.Maui.Core" Version="34.1.29" />
		<PackageReference Include="Syncfusion.Maui.DataGrid" Version="34.1.29" />
		<PackageReference Include="Syncfusion.Maui.Charts" Version="34.1.29" />
		<PackageReference Include="Syncfusion.Maui.Scheduler" Version="34.1.29" />
		<PackageReference Include="Syncfusion.Maui.ListView" Version="34.1.29" />
		<PackageReference Include="Syncfusion.Maui.Inputs" Version="34.1.29" />
		<PackageReference Include="Syncfusion.Maui.Buttons" Version="34.1.29" />
		<PackageReference Include="Syncfusion.Maui.TabView" Version="34.1.29" />
		<PackageReference Include="Syncfusion.Maui.Popup" Version="34.1.29" />
		<PackageReference Include="Syncfusion.Maui.Calendar" Version="34.1.29" />
		
		<!-- Syncfusion PDF Library - For generating PDF receipts and reports -->
		<PackageReference Include="Syncfusion.Pdf.Net.Core" Version="34.1.29" />
		<!-- Note: PdfViewer.Maui doesn't exist as separate package - PDF generation only -->
	</ItemGroup>

	<!-- Exclude Windows-specific dependencies that cause crashes on macOS -->
	<ItemGroup>
		<RuntimeHostConfigurationOption Include="System.Drawing.EnableUnixSupport" Value="true" />
	</ItemGroup>
	
	<!-- Exclude problematic Windows assemblies -->
	<Target Name="RemoveWindowsSpecificAssemblies" AfterTargets="ResolveAssemblyReferences">
		<ItemGroup>
			<ReferencePath Remove="@(ReferencePath)" Condition="'%(FileName)' == 'System.Drawing.Common'" />
			<ReferencePath Remove="@(ReferencePath)" Condition="'%(FileName)' == 'System.Windows.Extensions'" />
		</ItemGroup>
	</Target>

</Project>
