<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:POS_in_NET.Views"
             xmlns:controls="clr-namespace:POS_in_NET.Controls"
             x:Class="POS_in_NET.Pages.PostcodeLookupPage"
             Title=""
             BackgroundColor="{StaticResource Background}">

    <Grid RowDefinitions="Auto,Auto,*">
        
        <!-- Top Bar -->
        <views:TopBar x:Name="TopBar" Grid.Row="0"/>
        
        <!-- Toast Notification Overlay -->
        <controls:ToastNotification x:Name="ToastNotification" 
                                   Grid.RowSpan="3"
                                   ZIndex="1000"
                                   InputTransparent="True"/>
        
        <!-- Modern Tab Bar -->
        <Frame Grid.Row="1" 
               BackgroundColor="White"
               Padding="0"
               CornerRadius="0"
               HasShadow="False"
               BorderColor="#E5E7EB">
            <ScrollView Orientation="Horizontal" HorizontalScrollBarVisibility="Never" Padding="20,16">
                <HorizontalStackLayout Spacing="8" HorizontalOptions="Center">
                    <!-- Business Info Tab -->
                    <Border x:Name="BusinessTabBorder"
                            BackgroundColor="White"
                            StrokeThickness="1"
                            Stroke="#E5E7EB"
                            Padding="24,12"
                            MinimumWidthRequest="140"
                            HeightRequest="48">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="12"/>
                        </Border.StrokeShape>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnBusinessTabClicked"/>
                        </Border.GestureRecognizers>
                        <Label Text="Business Info" 
                               TextColor="#6B7280"
                               FontSize="14"
                               FontAttributes="Bold"
                               VerticalOptions="Center"
                               HorizontalOptions="Center"/>
                    </Border>

                    <!-- User Tab -->
                    <Border x:Name="UserTabBorder"
                            BackgroundColor="White"
                            StrokeThickness="1"
                            Stroke="#E5E7EB"
                            Padding="24,12"
                            MinimumWidthRequest="100"
                            HeightRequest="48">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="12"/>
                        </Border.StrokeShape>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnUserTabClicked"/>
                        </Border.GestureRecognizers>
                        <Label Text="User" 
                               TextColor="#6B7280"
                               FontSize="14"
                               FontAttributes="Bold"
                               VerticalOptions="Center"
                               HorizontalOptions="Center"/>
                    </Border>

                    <!-- OrderWeb Connect Tab -->
                    <Border x:Name="CloudTabBorder"
                            BackgroundColor="White"
                            StrokeThickness="1"
                            Stroke="#E5E7EB"
                            Padding="24,12"
                            MinimumWidthRequest="160"
                            HeightRequest="48">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="12"/>
                        </Border.StrokeShape>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnCloudTabClicked"/>
                        </Border.GestureRecognizers>
                        <Label Text="OrderWeb Connect" 
                               TextColor="#6B7280"
                               FontSize="14"
                               FontAttributes="Bold"
                               VerticalOptions="Center"
                               HorizontalOptions="Center"/>
                    </Border>

                    <!-- Postcode Lookup Tab (Active) -->
                    <Border x:Name="PostcodeTabBorder"
                            BackgroundColor="#FEF2F2"
                            StrokeThickness="2"
                            Stroke="#EF4444"
                            Padding="24,12"
                            MinimumWidthRequest="150"
                            HeightRequest="48">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="12"/>
                        </Border.StrokeShape>
                        <Border.Shadow>
                            <Shadow Brush="#EF4444" Radius="8" Opacity="0.2" Offset="0,2"/>
                        </Border.Shadow>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnPostcodeTabClicked"/>
                        </Border.GestureRecognizers>
                        <Label Text="Postcode Lookup" 
                               TextColor="#EF4444"
                               FontSize="14"
                               FontAttributes="Bold"
                               VerticalOptions="Center"
                               HorizontalOptions="Center"/>
                    </Border>
                </HorizontalStackLayout>
            </ScrollView>
        </Frame>
        
        <!-- Main Content -->
        <ScrollView Grid.Row="2">
        <VerticalStackLayout Padding="32" Spacing="24" MaximumWidthRequest="1400" HorizontalOptions="Center">

            <!-- Header Section -->
            <VerticalStackLayout Spacing="8" Margin="0,0,0,8">
                <Label Text="UK Address Lookup"
                       FontSize="28"
                       FontFamily="OpenSansBold"
                       TextColor="#1F2937" />
                <Label Text="OrderWeb platform API (shared owp_ key for all restaurants)"
                       FontSize="15"
                       FontFamily="OpenSansRegular"
                       TextColor="#6B7280" />
            </VerticalStackLayout>

            <Frame BackgroundColor="White"
                   CornerRadius="12"
                   Padding="24"
                   HasShadow="False"
                   BorderColor="#E5E7EB">
                <VerticalStackLayout Spacing="16">
                    <Label Text="OrderWeb Address API"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="#1F2937"/>

                    <VerticalStackLayout Spacing="6">
                        <Label Text="Base URL" FontSize="14" FontAttributes="Bold" TextColor="#374151"/>
                        <Entry x:Name="OrderWebBaseUrlEntry"
                               Placeholder="https://orderweb.net"
                               FontSize="14"
                               TextColor="#1F2937"/>
                    </VerticalStackLayout>

                    <VerticalStackLayout Spacing="6">
                        <HorizontalStackLayout Spacing="8">
                            <Label Text="API Key (owp_...)" FontSize="14" FontAttributes="Bold" TextColor="#374151" VerticalOptions="Center"/>
                            <Button x:Name="ToggleOrderWebApiKeyButton"
                                    Text="Show"
                                    FontSize="12"
                                    Padding="8,4"
                                    BackgroundColor="#E5E7EB"
                                    TextColor="#374151"
                                    CornerRadius="6"
                                    Clicked="OnToggleOrderWebApiKey"/>
                        </HorizontalStackLayout>
                        <Entry x:Name="OrderWebAddressApiKeyEntry"
                               Placeholder="owp_..."
                               FontSize="14"
                               IsPassword="True"
                               BackgroundColor="White"
                               TextColor="#1F2937"/>
                    </VerticalStackLayout>

                    <HorizontalStackLayout Spacing="8">
                        <Button x:Name="TestOrderWebButton"
                                Text="Test"
                                Clicked="OnTestOrderWebClicked"
                                BackgroundColor="#3B82F6"
                                TextColor="White"
                                CornerRadius="8"
                                HeightRequest="44"
                                FontSize="14"
                                FontAttributes="Bold"/>
                    </HorizontalStackLayout>

                    <Frame x:Name="OrderWebStatusFrame"
                           Padding="14"
                           CornerRadius="8"
                           IsVisible="False"
                           HasShadow="False"
                           BorderColor="#E5E7EB">
                        <Label x:Name="OrderWebStatusText"
                               FontSize="13"
                               LineBreakMode="WordWrap"/>
                    </Frame>

                    <Frame x:Name="OrderWebStatsFrame"
                           Padding="16"
                           BackgroundColor="#F3F4F6"
                           BorderColor="#E5E7EB"
                           CornerRadius="8"
                           IsVisible="False"
                           HasShadow="False">
                        <VerticalStackLayout Spacing="6">
                            <Label Text="Usage on this terminal"
                                   FontSize="13"
                                   FontAttributes="Bold"
                                   TextColor="#374151"/>
                            <Label x:Name="OrderWebUsageLabel"
                                   FontSize="12"
                                   TextColor="#6B7280"/>
                            <Label x:Name="OrderWebLastUsedLabel"
                                   FontSize="11"
                                   TextColor="#9CA3AF"/>
                        </VerticalStackLayout>
                    </Frame>
                </VerticalStackLayout>
            </Frame>

            <!-- Action Buttons -->
            <Grid ColumnDefinitions="*,*" ColumnSpacing="12" Margin="0,8,0,20">
                <Button Grid.Column="0"
                        x:Name="CancelButton"
                        Text="Cancel"
                        Clicked="OnCancelClicked"
                        BackgroundColor="#6B7280"
                        TextColor="White"
                        CornerRadius="8"
                        HeightRequest="48"
                        FontSize="15"
                        FontAttributes="Bold"/>

                <Button Grid.Column="1"
                        x:Name="SaveButton"
                        Text="Save Settings"
                        Clicked="OnSaveClicked"
                        BackgroundColor="#10B981"
                        TextColor="White"
                        CornerRadius="8"
                        HeightRequest="48"
                        FontSize="15"
                        FontAttributes="Bold"/>
            </Grid>

            <!-- Activity Indicator -->
            <ActivityIndicator x:Name="LoadingIndicator"
                             IsRunning="False"
                             IsVisible="False"
                             Color="#3B82F6"
                             HeightRequest="40"
                             WidthRequest="40"
                             HorizontalOptions="Center"/>

        </VerticalStackLayout>
    </ScrollView>
    
    </Grid>
</ContentPage>
