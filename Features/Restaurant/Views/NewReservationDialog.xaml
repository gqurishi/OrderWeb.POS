<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="POS_in_NET.Views.NewReservationDialog"
             BackgroundColor="{StaticResource DialogOverlayColor}"
             VerticalOptions="Fill"
             HorizontalOptions="Fill">

    <Grid VerticalOptions="Center"
          HorizontalOptions="Center"
          Padding="32">
        <Border BackgroundColor="White"
                Stroke="{StaticResource DialogShellStrokeColor}"
                StrokeThickness="1"
                Padding="28"
                WidthRequest="680"
                MaximumHeightRequest="780"
                VerticalOptions="Center"
                HorizontalOptions="Center">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="18"/>
            </Border.StrokeShape>
            <Border.Shadow>
                <Shadow Brush="#0F172A" Opacity="0.14" Radius="18" Offset="0,6"/>
            </Border.Shadow>

            <Grid RowDefinitions="Auto,Auto,*,Auto" RowSpacing="16">
                <Grid ColumnDefinitions="*,Auto">
                    <VerticalStackLayout Spacing="4">
                        <Label Text="New Reservation"
                               FontSize="24"
                               FontAttributes="Bold"
                               TextColor="#0F172A" />
                        <Label Text="Add a booking to the diary"
                               FontSize="14"
                               TextColor="#64748B" />
                    </VerticalStackLayout>
                    <Button Grid.Column="1"
                            Text="X"
                            WidthRequest="40"
                            HeightRequest="40"
                            CornerRadius="20"
                            BackgroundColor="#FEE2E2"
                            TextColor="#DC2626"
                            FontSize="18"
                            Padding="0"
                            Clicked="OnCancelClicked" />
                </Grid>

                <Grid Grid.Row="1"
                      ColumnDefinitions="*,*"
                      ColumnSpacing="14"
                      RowDefinitions="Auto"
                      Padding="0">
                    <VerticalStackLayout Grid.Column="0" Spacing="6">
                        <Label Text="Booking date" FontSize="13" FontAttributes="Bold" TextColor="#475569" />
                        <Border BackgroundColor="#EFF6FF"
                                Stroke="#BFDBFE"
                                StrokeThickness="1"
                                StrokeShape="RoundRectangle 12"
                                Padding="16,14">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnDateFieldTapped" />
                            </Border.GestureRecognizers>
                            <Grid ColumnDefinitions="*,Auto">
                                <Label x:Name="DateDisplayLabel"
                                       FontSize="17"
                                       TextColor="#0F172A"
                                       VerticalOptions="Center" />
                                <Label Grid.Column="1"
                                       Text="Change"
                                       FontSize="12"
                                       FontAttributes="Bold"
                                       TextColor="#2563EB"
                                       VerticalOptions="Center" />
                            </Grid>
                        </Border>
                    </VerticalStackLayout>
                    <VerticalStackLayout Grid.Column="1" Spacing="6">
                        <Label Text="Booking time" FontSize="13" FontAttributes="Bold" TextColor="#475569" />
                        <Border BackgroundColor="#EFF6FF"
                                Stroke="#BFDBFE"
                                StrokeThickness="1"
                                StrokeShape="RoundRectangle 12"
                                Padding="16,14">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnTimeFieldTapped" />
                            </Border.GestureRecognizers>
                            <Grid ColumnDefinitions="*,Auto">
                                <Label x:Name="TimeDisplayLabel"
                                       FontSize="17"
                                       TextColor="#0F172A"
                                       VerticalOptions="Center" />
                                <Label Grid.Column="1"
                                       Text="Change"
                                       FontSize="12"
                                       FontAttributes="Bold"
                                       TextColor="#2563EB"
                                       VerticalOptions="Center" />
                            </Grid>
                        </Border>
                    </VerticalStackLayout>
                </Grid>

                <ScrollView Grid.Row="2">
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="14" RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto" RowSpacing="12">
                        <VerticalStackLayout Grid.Row="0" Grid.Column="0" Spacing="6">
                            <Label Text="Covers" FontSize="13" FontAttributes="Bold" TextColor="#475569" />
                            <Border BackgroundColor="#F8FAFC" Stroke="#E2E8F0" StrokeShape="RoundRectangle 10" Padding="12,4">
                                <Entry x:Name="CoversEntry" Keyboard="Numeric" Text="2" FontSize="16" BackgroundColor="Transparent" />
                            </Border>
                        </VerticalStackLayout>

                        <VerticalStackLayout Grid.Row="0" Grid.Column="1" Spacing="6">
                            <Label Text="Table (optional)" FontSize="13" FontAttributes="Bold" TextColor="#475569" />
                            <Border BackgroundColor="#F8FAFC" Stroke="#E2E8F0" StrokeShape="RoundRectangle 10" Padding="12,4">
                                <Entry x:Name="TableEntry" Placeholder="e.g. T4" FontSize="16" BackgroundColor="Transparent" />
                            </Border>
                        </VerticalStackLayout>

                        <VerticalStackLayout Grid.Row="1" Grid.ColumnSpan="2" Spacing="6">
                            <Label Text="Full name" FontSize="13" FontAttributes="Bold" TextColor="#475569" />
                            <Border BackgroundColor="#F8FAFC" Stroke="#E2E8F0" StrokeShape="RoundRectangle 10" Padding="12,4">
                                <Entry x:Name="NameEntry" Placeholder="Guest name" FontSize="16" BackgroundColor="Transparent" />
                            </Border>
                        </VerticalStackLayout>

                        <VerticalStackLayout Grid.Row="2" Grid.Column="0" Spacing="6">
                            <Label Text="Phone" FontSize="13" FontAttributes="Bold" TextColor="#475569" />
                            <Border BackgroundColor="#F8FAFC" Stroke="#E2E8F0" StrokeShape="RoundRectangle 10" Padding="12,4">
                                <Entry x:Name="PhoneEntry" Keyboard="Telephone" Placeholder="07..." FontSize="16" BackgroundColor="Transparent" />
                            </Border>
                        </VerticalStackLayout>

                        <VerticalStackLayout Grid.Row="2" Grid.Column="1" Spacing="6">
                            <Label Text="Email (optional)" FontSize="13" FontAttributes="Bold" TextColor="#475569" />
                            <Border BackgroundColor="#F8FAFC" Stroke="#E2E8F0" StrokeShape="RoundRectangle 10" Padding="12,4">
                                <Entry x:Name="EmailEntry" Keyboard="Email" Placeholder="you@example.com" FontSize="16" BackgroundColor="Transparent" />
                            </Border>
                        </VerticalStackLayout>

                        <VerticalStackLayout Grid.Row="3" Grid.ColumnSpan="2" Spacing="6">
                            <Label Text="Promocode (optional)" FontSize="13" FontAttributes="Bold" TextColor="#475569" />
                            <Border BackgroundColor="#F8FAFC" Stroke="#E2E8F0" StrokeShape="RoundRectangle 10" Padding="12,4">
                                <Entry x:Name="PromoCodeEntry" Placeholder="e.g. SUMMER10" FontSize="16" BackgroundColor="Transparent" />
                            </Border>
                        </VerticalStackLayout>

                        <VerticalStackLayout Grid.Row="4" Grid.ColumnSpan="2" Spacing="6">
                            <Label Text="Allergies / dietary (optional)" FontSize="13" FontAttributes="Bold" TextColor="#475569" />
                            <Border BackgroundColor="#F8FAFC" Stroke="#E2E8F0" StrokeShape="RoundRectangle 10" Padding="12,4">
                                <Editor x:Name="AllergiesEditor" AutoSize="TextChanges" MinimumHeightRequest="58" Placeholder="e.g. nut allergy, gluten free" FontSize="15" BackgroundColor="Transparent" />
                            </Border>
                        </VerticalStackLayout>

                        <VerticalStackLayout Grid.Row="5" Grid.ColumnSpan="2" Spacing="6">
                            <Label Text="Special requests (optional)" FontSize="13" FontAttributes="Bold" TextColor="#475569" />
                            <Border BackgroundColor="#F8FAFC" Stroke="#E2E8F0" StrokeShape="RoundRectangle 10" Padding="12,4">
                                <Editor x:Name="NotesEditor" AutoSize="TextChanges" MinimumHeightRequest="58" Placeholder="High chair, birthday, window table..." FontSize="15" BackgroundColor="Transparent" />
                            </Border>
                        </VerticalStackLayout>
                    </Grid>
                </ScrollView>

                <Grid Grid.Row="3" ColumnDefinitions="*,*" ColumnSpacing="12" Margin="0,8,0,0">
                    <Button Grid.Column="0"
                            Text="Cancel"
                            BackgroundColor="#F1F5F9"
                            TextColor="#64748B"
                            CornerRadius="12"
                            HeightRequest="48"
                            Clicked="OnCancelClicked" />
                    <Button Grid.Column="1"
                            Text="Save Booking"
                            BackgroundColor="#2563EB"
                            TextColor="White"
                            FontAttributes="Bold"
                            CornerRadius="12"
                            HeightRequest="48"
                            Clicked="OnSaveClicked" />
                </Grid>
            </Grid>
        </Border>

        <Grid x:Name="PickerOverlay"
              IsVisible="False"
              BackgroundColor="#660F172A">
            <Border BackgroundColor="White"
                    Stroke="#CBD5E1"
                    StrokeThickness="1"
                    StrokeShape="RoundRectangle 18"
                    WidthRequest="430"
                    Padding="18"
                    HorizontalOptions="Center"
                    VerticalOptions="Center">
                <Border.Shadow>
                    <Shadow Brush="#0F172A" Opacity="0.18" Radius="18" Offset="0,8" />
                </Border.Shadow>

                <Grid>
                    <Grid x:Name="DatePickerPanel"
                          RowDefinitions="Auto,Auto,Auto,Auto"
                          RowSpacing="14"
                          IsVisible="False">
                        <Grid ColumnDefinitions="Auto,*,Auto,Auto"
                              ColumnSpacing="8">
                            <Button Text="‹"
                                    WidthRequest="42"
                                    HeightRequest="38"
                                    CornerRadius="12"
                                    BackgroundColor="#F1F5F9"
                                    TextColor="#0F172A"
                                    Clicked="OnPreviousMonthClicked" />
                            <Label x:Name="PickerMonthLabel"
                                   Grid.Column="1"
                                   FontSize="18"
                                   FontAttributes="Bold"
                                   TextColor="#0F172A"
                                   HorizontalTextAlignment="Center"
                                   VerticalOptions="Center" />
                            <Button Grid.Column="2"
                                    Text="›"
                                    WidthRequest="42"
                                    HeightRequest="38"
                                    CornerRadius="12"
                                    BackgroundColor="#F1F5F9"
                                    TextColor="#0F172A"
                                    Clicked="OnNextMonthClicked" />
                            <Button Grid.Column="3"
                                    Text="X"
                                    WidthRequest="38"
                                    HeightRequest="38"
                                    CornerRadius="19"
                                    BackgroundColor="#FEE2E2"
                                    TextColor="#DC2626"
                                    FontSize="16"
                                    Padding="0"
                                    Clicked="OnPickerCancelClicked" />
                        </Grid>

                        <Grid Grid.Row="1" ColumnDefinitions="*,*,*,*,*,*,*">
                            <Label Text="Mon" FontSize="12" FontAttributes="Bold" TextColor="#64748B" HorizontalTextAlignment="Center" />
                            <Label Grid.Column="1" Text="Tue" FontSize="12" FontAttributes="Bold" TextColor="#64748B" HorizontalTextAlignment="Center" />
                            <Label Grid.Column="2" Text="Wed" FontSize="12" FontAttributes="Bold" TextColor="#64748B" HorizontalTextAlignment="Center" />
                            <Label Grid.Column="3" Text="Thu" FontSize="12" FontAttributes="Bold" TextColor="#64748B" HorizontalTextAlignment="Center" />
                            <Label Grid.Column="4" Text="Fri" FontSize="12" FontAttributes="Bold" TextColor="#64748B" HorizontalTextAlignment="Center" />
                            <Label Grid.Column="5" Text="Sat" FontSize="12" FontAttributes="Bold" TextColor="#64748B" HorizontalTextAlignment="Center" />
                            <Label Grid.Column="6" Text="Sun" FontSize="12" FontAttributes="Bold" TextColor="#64748B" HorizontalTextAlignment="Center" />
                        </Grid>

                        <Grid x:Name="CalendarDaysGrid"
                              Grid.Row="2"
                              ColumnDefinitions="*,*,*,*,*,*,*"
                              RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto"
                              ColumnSpacing="6"
                              RowSpacing="6" />
                    </Grid>

                    <Grid x:Name="TimePickerPanel"
                          RowDefinitions="Auto,Auto,Auto,Auto"
                          RowSpacing="14"
                          IsVisible="False">
                        <Label Text="Choose booking time"
                               FontSize="20"
                               FontAttributes="Bold"
                               TextColor="#0F172A"
                               HorizontalTextAlignment="Center" />
                        <Border Grid.Row="1"
                                BackgroundColor="#EFF6FF"
                                Stroke="#BFDBFE"
                                StrokeShape="RoundRectangle 16"
                                Padding="16,12">
                            <Label x:Name="PickerTimePreviewLabel"
                                   FontSize="30"
                                   FontAttributes="Bold"
                                   TextColor="#0F172A"
                                   HorizontalTextAlignment="Center" />
                        </Border>
                        <Grid Grid.Row="2" ColumnDefinitions="*,*" ColumnSpacing="10">
                            <VerticalStackLayout Spacing="6">
                                <Label Text="Hour"
                                       FontSize="12"
                                       FontAttributes="Bold"
                                       TextColor="#64748B"
                                       HorizontalTextAlignment="Center" />
                                <Border BackgroundColor="#F8FAFC"
                                        Stroke="#E2E8F0"
                                        StrokeShape="RoundRectangle 14"
                                        Padding="6">
                                    <ScrollView HeightRequest="180">
                                        <VerticalStackLayout x:Name="HourOptionsStack" Spacing="6" />
                                    </ScrollView>
                                </Border>
                            </VerticalStackLayout>
                            <VerticalStackLayout Grid.Column="1" Spacing="6">
                                <Label Text="Minute"
                                       FontSize="12"
                                       FontAttributes="Bold"
                                       TextColor="#64748B"
                                       HorizontalTextAlignment="Center" />
                                <Border BackgroundColor="#F8FAFC"
                                        Stroke="#E2E8F0"
                                        StrokeShape="RoundRectangle 14"
                                        Padding="6">
                                    <ScrollView HeightRequest="180">
                                        <VerticalStackLayout x:Name="MinuteOptionsStack" Spacing="6" />
                                    </ScrollView>
                                </Border>
                            </VerticalStackLayout>
                        </Grid>
                        <Grid Grid.Row="3" ColumnDefinitions="*,*" ColumnSpacing="12">
                            <Button Text="Cancel"
                                    HeightRequest="46"
                                    CornerRadius="12"
                                    BackgroundColor="#F1F5F9"
                                    TextColor="#64748B"
                                    Clicked="OnPickerCancelClicked" />
                            <Button Grid.Column="1"
                                    Text="Use Time"
                                    HeightRequest="46"
                                    CornerRadius="12"
                                    FontAttributes="Bold"
                                    BackgroundColor="#2563EB"
                                    TextColor="White"
                                    Clicked="OnPickerDoneClicked" />
                        </Grid>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</ContentView>
